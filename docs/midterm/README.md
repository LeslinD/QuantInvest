# 中期文档

这里是中期阶段的文档入口。

## 内容

- `reports/`：中期报告正文、Word 版本和中期汇报逐页 Markdown。
- `presentations/`：中期汇报 PPT 成品。
- `planning/`：中期实验设计和可实施方案文档。
- `reference/`：中期报告参考模板。

完整冻结版同时保存在 `artifacts/midterm/`，后续迭代时优先把那里当作不可变归档。
